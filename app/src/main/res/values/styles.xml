<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme and other styles -->

    <!-- Custom Button Style -->
    <style name="CustomButton" parent="Widget.AppCompat.Button">
        <!-- Set custom background (you can define it in a drawable XML) -->
        <item name="android:background">@drawable/btncorner</item>

        <!-- Text appearance for the button -->
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">18sp</item>

        <!-- Padding for the button -->
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>

        <!-- Elevation/shadow for Material-style buttons -->
        <item name="android:elevation">4dp</item>
    </style>

</resources>
